@model asyncMVC.Models.EmployeeModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<table>
    <tr>
        <td>ID:</td>
        <td>@Model.id</td>
    </tr>
    <tr>
        <td>Name:</td>
        <td>@Model.name</td>
    </tr>
    <tr>
        <td>Salary:</td>
        <td>@Model.salary</td>
    </tr>

    @Html.ActionLink("Create", "Create", "Home")
</table>

